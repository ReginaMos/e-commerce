export interface PromotionModel {
  heading: string;
  text: string;
  img: string;
}

export type LinkItem = {
  NAME: string;
  LINK: string;
};

export interface Advantage {
  heading: string;
  text: string;
  icon: string;
}
